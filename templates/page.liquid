{% comment %}
  This is the page template for the Shopify AI Theme Dev project
{% endcomment %}

<div class="page-width">
  <div class="section-header text-center">
    <h1>{{ page.title }}</h1>
  </div>

  <div class="page-content">
    {{ page.content }}
  </div>
</div>

<style>
  .page-width {
    padding: 55px 0;
  }
  
  .page-content {
    margin-top: 30px;
  }
</style>
